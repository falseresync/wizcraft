package falseresync.wizcraft.common.item.focus;

public enum FocusPlating {
    IRON(0),
    GOLD(1),
    COPPER(2);

    public final int index;

    FocusPlating(int index) {
        this.index = index;
    }
}
